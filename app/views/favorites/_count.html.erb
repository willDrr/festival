<span>Favorite Count:</span>
<%# no src here to avoid infinite loop, page 35 from book%>
<%= turbo_frame_tag("favorites-count-number") do %>
  <span id="favorites-count" class="ml-2">
    <%= current_user&.favorites.count || 0 %> <%# no count for nil class template error %>
  </span>
<% end %>